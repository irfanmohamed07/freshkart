<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 min-h-screen">
    <%- include('partials/header') %>

    <main class="container mx-auto p-4 mt-8 flex-1">
        <section class="bg-white p-6 rounded-lg shadow-md">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-semibold">Welcome to our Grocery E-commerce App</h2>
                <div class="flex space-x-4">
                    <a href="/" class="flex items-center text-green-600 hover:text-green-700">
                        <i class="fas fa-home text-xl"></i>
                        <span class="ml-1 hidden sm:inline">Home</span>
                    </a>
                    <a href="/cart" class="flex items-center text-green-600 hover:text-green-700 relative">
                        <i class="fas fa-shopping-cart text-xl"></i>
                        <span class="ml-1 hidden sm:inline">Cart</span>
                        <% if (locals.user && locals.cartCount) { %>
                            <span class="absolute -top-2 -right-2 bg-green-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                                <%= locals.cartCount %>
                            </span>
                        <% } else if (locals.user) { %>
                            <span class="absolute -top-2 -right-2 bg-green-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                                0
                            </span>
                        <% } %>
                    </a>
                </div>
            </div>
            <% if (locals.user) { %>
                <p class="mb-4">Hello, <%= user.name %>! Find the best deals on groceries from various shops in your area.</p>
            <% } else { %>
                <p class="mb-4">Find the best deals on groceries from various shops in your area.</p>
            <% } %>
            <div class="mt-6">
                <a href="/shops" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">Browse Shops</a>
            </div>
        </section>
    </main>

    <%- include('partials/footer') %>
</body>
</html> 